
# Updated section for your main.py

# Change this line:
# model = YOLO("../models/yolov8n.pt")

# To this:
model = YOLO("./models/license_plate_model/weights/best.pt")

# The model will now detect license plates instead of general objects!
# You may need to adjust the confidence threshold:
# results = model.predict(img_bgr, conf=0.25)  # Lower than 0.3 if needed
